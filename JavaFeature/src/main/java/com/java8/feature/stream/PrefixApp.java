package com.java8.feature.stream;

public class PrefixApp {

    public static void main(String[] args) {
        //String findCommonPrefix();

    }
    // Q.write code to find the prefix which is common in all the array of string?
    /*public static String findCommonPrefix(String[] strs)
    { if(strs != null || strs.length <0){ return ""; }
        String prefix = strs[0]; for (int i = 0; i < strs.length; i++)
    { while(strs[i].indexOf(prefix) != 0){ prefix = prefix.substring(0,prefix.length()-1);
        if(prefix.isEmpty()){ return ""; } } } return prefix; } public static void main(String[] args) { String[] strs = {"flower", "flow", "flight"}; E e = new E(); e.findCommonPrefix(strs); System.out.println(e.findCommonPrefix(strs)); } }
*/
}
